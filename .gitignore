# Exclude everything by default

DOMAIN-IDEAS.md

# Then whitelist only what we want to track
!src/
!src/**/*.ts
!package.json
!tsconfig.json
!README.md
!.gitignore
# Always ignore these, even if they're in a whitelisted directory
node_modules/
.tmp/
GEMINI.md
.temp/
backup/
build/
dist/
docs-infos/
imgs/
*copy*
*config.providersTEST*
*.log
bun.lockb
package-lock.json
# Configuration directories avec clés API (SÉCURISÉ)
# Option 1: Dans le projet (peut poser problème avec certains IDEs)
.gemini/settings.json
.gemini/*.env
.kiro/settings/
.kiro/settings/mcp.json 
.roo/
.roo/settings/
.roo/mcp.json
.serena/settings/
.specify/settings/
.claude/
.cursor/
.cline/

# Permettre les templates publics dans .gemini
!.gemini/settings.template.json
!.gemini/README.md

# Option 2: Configurations externes (recommandé pour éviter gitignore)
mcp-configs/

# Fichiers de configuration avec secrets
*settings.json
!*settings.template.json
*config.json
*.env*
!.env.example

# files with certain keywords
*copy*
*secret*
*token*
*api-key*
*github_pat_*
*GITHUB_TOKEN*
*API_KEY*
*BRAVE_API_KEY*

# Fichiers MCP avec tokens
mcp.json
*/mcp.json
**/*mcp*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.aider*